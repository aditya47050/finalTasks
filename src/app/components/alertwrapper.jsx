"use client"

import React from "react"
import RegistrationAlertDialog from "./registration-alert-dialog"

export default function RegistrationAlertWrapper(props) {
  return <RegistrationAlertDialog {...props} />;
}
